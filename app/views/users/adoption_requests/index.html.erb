<h1>Requests</h1>

<% flash.each do |type, msg| %>
  <div class="<%= type %>">
    <%= msg %>
  </div>
<% end %>

<ul>
  <% @requests.each do |r| %>
  <li>[<%= r.state %>] Request: <%= r.uid %> by <%= r.requester %></li>
  <% end %>
</ul>
